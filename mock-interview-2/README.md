# Mock Interview 2

Low-level design practice: **mock-interview-2**

## Run

From the repo root:

```bash
./gradlew :mock-interview-2:run
```

Or from this folder:

```bash
../gradlew run
```

## Package

`com.saurav.lld.mockinterview2` — keep all types for this problem in this package (or subpackages under it) so they never clash with other problems.

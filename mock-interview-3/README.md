# Mock Interview 3

Low-level design practice: **mock-interview-3**

## Run

From the repo root:

```bash
./gradlew :mock-interview-3:run
```

Or from this folder:

```bash
../gradlew run
```

## Package

`com.saurav.lld.mockinterview3` — keep all types for this problem in this package (or subpackages under it) so they never clash with other problems.

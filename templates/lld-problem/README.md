# {{PROBLEM_TITLE}}

Low-level design practice: **{{PROBLEM_SLUG}}**

## Run

From the repo root:

```bash
./gradlew :{{PROBLEM_SLUG}}:run
```

Or from this folder:

```bash
../gradlew run
```

## Package

`{{PACKAGE_NAME}}` — keep all types for this problem in this package (or subpackages under it) so they never clash with other problems.

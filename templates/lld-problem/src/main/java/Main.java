package {{PACKAGE_NAME}};

/**
 * Entry point for {{PROBLEM_SLUG}}.
 * Add domain types in this package (or subpackages), not in the default package.
 */
public class Main {

    public static void main(String[] args) {
        System.out.println("{{PROBLEM_SLUG}} ready.");
    }
}
